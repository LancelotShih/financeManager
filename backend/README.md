# FinanceAppV2 Backend

This is the FastAPI backend for the FinanceAppV2 project.

- REST API for retirement, treasuries, and cash accounts
- SQLite for persistent storage

## Getting Started
Install dependencies and run the server:

```bash
pip install -r requirements.txt
uvicorn main:app --reload
```
